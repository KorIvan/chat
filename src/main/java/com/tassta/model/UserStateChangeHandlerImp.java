package com.tassta.model;

public class UserStateChangeHandlerImp implements UserStateChangeHandler {

	@Override
	public void handle(int id, User newValue) {
	//	((UserImpl)newValue).setIsOnline(!newValue.isOnline());
	}

}
